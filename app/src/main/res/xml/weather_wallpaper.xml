<?xml version="1.0" encoding="utf-8"?>
<!--
  Live wallpaper service metadata for WeatherWallpaperService.

  TODO(N-4 / N-1): Expose WallpaperDescription on compileSdk 36 so the weather
  engine can declare separate instances per WeatherEffect (rain/snow/fog/clear)
  through the Android 16 wallpaper picker. See ROADMAP.md N-1 + N-4.
-->
<wallpaper
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:label="Aura Weather"
    android:description="@string/weather_wallpaper_desc"
    android:thumbnail="@mipmap/ic_launcher" />
